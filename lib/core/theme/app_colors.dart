import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Backgrounds
  static const Color background = Color(0xFF050B12);
  static const Color surface = Color(0xFF111C28);
  static const Color surfaceLight = Color(0xFF1A2633);

  // Primary
  static const Color primary = Color(0xFF00BCD4);
  static const Color primaryLight = Color(0xFF00E5FF);

  // Premium
  static const Color gold = Color(0xFFFFC107);

  // Status
  static const Color success = Color(0xFF4CAF50);
  static const Color danger = Color(0xFFE53935);
  static const Color warning = Color(0xFFFF9800);

  // Text
  static const Color text = Colors.white;
  static const Color textSecondary = Colors.white70;
  static const Color textHint = Colors.white54;

  // Borders
  static const Color border = Color(0x33FFFFFF);
}